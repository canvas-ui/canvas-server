SynapsD indexes apps, files, roles, browser tabs and dotfiles - to name a few usual suspects - across several devices. Every device where a canvas-integrated application is running has to have a device ID and app ID. User should be able to pick and name his devices (lets say "work laptop", "ws@office", "nas@home" etc
1. Should we use a device.name + device.id and and let it be UUID or node-device ID for electron apps or should the name become the ID? I bet the answer is device.name as user-controlled data and device.id as a uuid or some more apropriate generated ID

1. a) for our webui we would probably name the device as the fqdn we are running on - canvas.idnc.sk for example

2. For app IDs, not sure whether a split like above is needed, if my obisidian plugin hard-codes "canvas-obsidian", paired with a properly generated and shared (among all local apps - which may require a small daemon - maybe a minimal canvas-server in client mode - running on a system) - we'd end up with something like canvas-obsidian@device.id or a huiman readable version canvas-obsidian@device.name


SynapsD is a small db module created for the Canvas application. We should support storable "views" - these are needed to support canvases as standalone elements that can be created within a context tree and hold the current context url, feature and filter arrays and I assume metadata which would hold more app specific settings like UI layout data or sharing options. The idea is to create a canvas in /work/customer/devops called jira-1234, bind specific browser tabs, notes, files or email threads to it and for easy access via REST - I should be able to filter what data are available in a given canvas(lets say I only want notes and browser tabs), I will be able to setup the UI layout as well (using applets for different data types) - and most importantly, I should be able to share such a canvas with ro wo or rw access. We already have internal @src/schemas/internal/layers  but maybe we should create a "views" category instead and create views/canvas? I will need to cleanup several other abstractions as those are either not generic enough for the module or assigned incorrectly but for now lets discuss the above
